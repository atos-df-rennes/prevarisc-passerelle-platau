name: CI

on:
  pull_request:
  push:
    branches:
      - master
      - develop

jobs:
  quality:
    name: "PHP ${{ matrix.php }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: [ '8.3', '8.4', '8.5' ]
        include:
          - php: '8.3'
            allow-failure: false
          - php: '8.4'
            allow-failure: true
          - php: '8.5'
            allow-failure: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP ${{ matrix.php }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, xml, dom, opcache
          coverage: none
          tools: composer:v2

      - name: Get composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> "$GITHUB_OUTPUT"

      - name: Cache composer dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: composer-${{ matrix.php }}-${{ hashFiles('composer.lock') }}
          restore-keys: |
            composer-${{ matrix.php }}-

      - name: Install dependencies
        id: install
        continue-on-error: ${{ matrix.allow-failure }}
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: Psalm
        if: steps.install.outcome == 'success'
        continue-on-error: ${{ matrix.allow-failure }}
        run: vendor/bin/psalm

      - name: PHP-CS-Fixer (dry-run, use minimum PHP version supported)
        if: steps.install.outcome == 'success' && matrix.php == '8.3'
        continue-on-error: ${{ matrix.allow-failure }}
        run: vendor/bin/php-cs-fixer fix --dry-run --diff --config=vendor/kdubuc/php-cs-fixer-rules/php-cs-fixer.php

      - name: Rector (dry-run)
        if: steps.install.outcome == 'success'
        continue-on-error: ${{ matrix.allow-failure }}
        run: vendor/bin/rector --dry-run

      - name: PHPUnit
        if: steps.install.outcome == 'success'
        continue-on-error: ${{ matrix.allow-failure }}
        run: vendor/bin/phpunit --testdox tests/
